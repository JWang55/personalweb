<h1>Post a new article</h1>
<%= form_for @article do |f| %>
  <p><%= f.text_field :title, placeholder:'article title' %></p>
  <p><%= f.text_field :proper, placeholder:'article body' %></p>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit %>
<% end %>